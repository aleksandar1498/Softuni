package factories;

import interfaces.engines.BoatEngine;
import interfaces.factories.Factory;
import utils.Engine;

public class EngineFactory implements Factory<BoatEngine> {
    @Override
    public BoatEngine build(String[] args) {
        return null;
    }
}
