package validator.validator.models.rules;

import validator.validator.interfaces.ValidationRule;

/**
 * Abstract class for format : e.g If the format is like PAT111 | DOC122
 * @param <T>
 */
public abstract class FormatRule<T> implements ValidationRule<T> {
}
